import { commonDataAccessTrpc } from './common-data-access-trpc';

describe('commonDataAccessTrpc', () => {
  it('should work', () => {
    expect(commonDataAccessTrpc()).toEqual('common-data-access-trpc');
  });
});
